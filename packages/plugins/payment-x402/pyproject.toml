[project]
name = "payment-x402"
version = "0.1.0"
description = "x402 HTTP-native payment adapter for Agent Adapter"
requires-python = ">=3.12,<3.14"
dependencies = [
    "agent-adapter-contracts",
    "x402>=2.6",
    "httpx>=0.28",
]

[tool.uv.sources]
agent-adapter-contracts = { workspace = true }
