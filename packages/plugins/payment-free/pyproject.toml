[project]
name = "payment-free"
version = "0.1.0"
description = "Free (no-op) payment adapter for Agent Adapter"
requires-python = ">=3.12,<3.14"
dependencies = [
    "agent-adapter-contracts",
]

[tool.uv.sources]
agent-adapter-contracts = { workspace = true }
